# Installation

Prometheus can be installed in two ways. Choose the method that works best for your setup:

- [Install with built-in installer](built-in-installer.md) — Linux and macOS. Builds Prometheus and its dependencies directly on your machine.
- [Install with Containers](containers.md) — Works on Windows, Intel-based macOS, and Linux. Recommended for computing clusters or if the built-in installer doesn't work for you.
